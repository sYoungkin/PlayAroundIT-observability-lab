---
# ============================================
#  Observability Lab - Metricbeat Deployment
#  Deploys and configures Metricbeat across
#  all lab nodes
# ============================================
- name: Gather facts from all hosts
  hosts: lab_all
  become: true
  gather_facts: true

# ----------------------------------------
#  Play 1 - Distribute Elasticsearch CA
#  cert to all Splunk nodes
# ----------------------------------------
- name: Distribute Elasticsearch CA certificate to Splunk nodes
  hosts: splunk_all
  become: true

  tasks:

    - name: Create certs directory on target node
      file:
        path: /etc/metricbeat/certs
        state: directory
        owner: root
        group: root
        mode: '0755'

    - name: Slurp CA cert from elastic node
      slurp:
        src: /etc/elasticsearch/certs/http_ca.crt
      delegate_to: elastic
      run_once: true
      register: ca_cert_content

    - name: Write CA cert to target node
      copy:
        content: "{{ ca_cert_content.content | b64decode }}"
        dest: /etc/metricbeat/certs/http_ca.crt
        owner: root
        group: root
        mode: '0644'

# ----------------------------------------
#  Play 2 - Install Metricbeat on all nodes
# ----------------------------------------
- name: Install and configure Metricbeat on all nodes
  hosts: lab_all
  become: true

  tasks:

    - name: Install apt dependencies
      apt:
        name:
          - wget
          - curl
          - gnupg
          - apt-transport-https
          - ca-certificates
        state: present
        update_cache: true

    - name: Add Elastic GPG key
      shell: |
        wget -qO - https://artifacts.elastic.co/GPG-KEY-elasticsearch \
          | gpg --dearmor -o /usr/share/keyrings/elasticsearch-keyring.gpg
      args:
        creates: /usr/share/keyrings/elasticsearch-keyring.gpg

    - name: Add Elastic APT repository
      apt_repository:
        repo: 'deb [signed-by=/usr/share/keyrings/elasticsearch-keyring.gpg] https://artifacts.elastic.co/packages/{{ metricbeat_version }}/apt stable main'
        state: present
        filename: "elastic-{{ metricbeat_version }}"

    - name: Install Metricbeat
      apt:
        name: metricbeat
        state: present
        update_cache: true

    - name: Configure Metricbeat output to Elasticsearch
      template:
        src: /etc/ansible/templates/metricbeat.yml.j2
        dest: /etc/metricbeat/metricbeat.yml
        owner: root
        group: root
        mode: '0644'

    - name: Enable system module
      command: metricbeat modules enable system
      args:
        creates: /etc/metricbeat/modules.d/system.yml

    - name: Enable linux module
      command: metricbeat modules enable linux
      args:
        creates: /etc/metricbeat/modules.d/linux.yml

    - name: Configure system module with additional metricsets
      copy:
        dest: /etc/metricbeat/modules.d/system.yml
        owner: root
        group: root
        mode: '0644'
        content: |
          - module: system
            period: 10s
            metricsets:
              - cpu
              - load
              - memory
              - network
              - process
              - process_summary
              - socket_summary
              - diskio
              - core
              - entropy
              - service
            process.include_top_n:
              by_cpu: 5
              by_memory: 5
            degrade_on_partial: false

          - module: system
            period: 1m
            metricsets:
              - filesystem
              - fsstat
            processors:
              - drop_event.when.regexp:
                  system.filesystem.mount_point: '^/(sys|cgroup|proc|dev|etc|host|lib|snap)($|/)'

          - module: system
            period: 5m
            metricsets:
              - uptime

    - name: Configure linux module with all metricsets
      copy:
        dest: /etc/metricbeat/modules.d/linux.yml
        owner: root
        group: root
        mode: '0644'
        content: |
          - module: linux
            period: 10s
            metricsets:
              - pageinfo
              - memory
              - iostat
              - conntrack
              - pressure
            enabled: true

    - name: Restart Metricbeat to apply module changes
      systemd:
        name: metricbeat
        state: restarted

    - name: Enable beat-xpack module
      command: metricbeat modules enable beat-xpack
      args:
        creates: /etc/metricbeat/modules.d/beat-xpack.yml

    - name: Enable and start Metricbeat
      systemd:
        name: metricbeat
        enabled: true
        state: started

# ----------------------------------------
#  Play 3 - Additional modules for elastic
#  node only
# ----------------------------------------
- name: Enable Elasticsearch and Kibana modules on elastic node
  hosts: elastic
  become: true

  tasks:

    - name: Enable elasticsearch-xpack module
      command: metricbeat modules enable elasticsearch-xpack
      args:
        creates: /etc/metricbeat/modules.d/elasticsearch-xpack.yml

    - name: Enable kibana-xpack module
      command: metricbeat modules enable kibana-xpack
      args:
        creates: /etc/metricbeat/modules.d/kibana-xpack.yml

    - name: Configure elasticsearch-xpack module
      copy:
        dest: /etc/metricbeat/modules.d/elasticsearch-xpack.yml
        owner: root
        group: root
        mode: '0644'
        content: |
          - module: elasticsearch
            xpack.enabled: true
            period: 10s
            hosts:
              - "https://localhost:9200"
            username: "{{ elasticsearch_username }}"
            password: "{{ elasticsearch_password }}"
            ssl.certificate_authorities:
              - "{{ elasticsearch_ca_cert }}"

    - name: Configure kibana-xpack module
      copy:
        dest: /etc/metricbeat/modules.d/kibana-xpack.yml
        owner: root
        group: root
        mode: '0644'
        content: |
          - module: kibana
            xpack.enabled: true
            period: 10s
            hosts:
              - "http://localhost:5601"
            username: "{{ elasticsearch_username }}"
            password: "{{ elasticsearch_password }}"

    - name: Run Metricbeat index management setup
      shell: metricbeat setup --index-management
      ignore_errors: true

    - name: Mark setup complete
      file:
        path: /var/lib/metricbeat/.setup_complete
        state: touch
        owner: root
        group: root
        mode: '0644'

    - name: Restart Metricbeat to pick up new modules
      systemd:
        name: metricbeat
        state: restarted
